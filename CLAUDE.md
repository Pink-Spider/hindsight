# Stock Yield History

주식 수익률 변동을 애니메이션 영상으로 만들어 YouTube Shorts로 발행하는 툴.

전체 설계 검토 문서: https://app.notion.com/p/3c28c64c554381788ebdc2b62380f20b
P0 실행 계획: https://app.notion.com/p/3c28c64c554381f9b3d4fb3a71ffbf31
ADR-001 컴포넌트 아키텍처(완성 시점): https://app.notion.com/p/3c28c64c55438164afdefdaaf640ea74
전체 플랜 P0~P5(단계별 실행 계획의 단일 원본): https://app.notion.com/p/3c28c64c554381a09bf4e98c6bed5ad1

## 무엇을 만드는가

티커 1~N개 + 기간(시작일~종료일) + 템플릿을 입력하면, 해당 기간의 수익률 변동을
애니메이션으로 그린다. mp4로 추출해 YouTube Shorts에 올린다.

- 단일 종목: 시작가 / 종가 / 수익률 표시
- 복수 종목: 각각의 수익률 + 종목 간 격차 표시
- 범위: 한국/미국 주식, ETF, 인덱스 지수

## 확정된 설계 결정

이 결정들은 검토를 거쳐 확정된 것이므로 임의로 바꾸지 말 것. 바꿔야 한다면 먼저 물어볼 것.

### 1. Scene Spec을 중간 산출물로 강제 분리

```
입력(티커[], 기간, 템플릿)
  → 데이터 수집 (어댑터: KR / US / FX)
  → 정규화 (수정주가, 공통 거래일 축, 리베이스 100)
  → Scene Spec (JSON)          ← ★ 렌더러와의 유일한 계약
  → 렌더러 A: Remotion → mp4   (오프라인 배치, 유튜브용)
  → 렌더러 B: 실시간 캔버스     (웹 미리보기 / 모바일 앱)
  → 업로더 (YouTube Data API)
```

- 렌더러 A와 B는 **같은 Scene Spec, 같은 템플릿 집합**을 지원해야 한다.
  이게 끊기면 웹에서 본 것과 앱에서 본 것이 달라진다.
- Spec에 `template`, `targetFps`, `maxPoints` 필드를 둔다.
- 다운샘플링(LTTB 등)은 **Spec 생성 단계**에서 처리한다. 렌더러는 이미 축약된
  시퀀스만 그린다.
- 다종목 Spec의 모든 시리즈는 **동일한 time 배열을 공유**해야 한다. 종목별 독립
  LTTB는 시점이 어긋나 격차 표시·바 차트 레이스가 깨지므로 금지.

### 2. 배포 형태

- 개인용 **로컬 웹앱**. Electron/Tauri 아님.
- 구성: 프론트 + 로컬 API 서버 + SQLite 캐시 + 렌더 워커, `docker compose up` 한 방.
- 모바일 앱(P5)은 **뷰어 전용** — mp4 저장 기능 없음. 서버 렌더 팜/인코딩 큐/
  결과물 스토리지가 전부 불필요.

### 3. 스택

- **데이터 수집: Python** — `FinanceDataReader` 1차, `yfinance` 2차 폴백.
  반드시 어댑터 인터페이스 뒤에 숨긴다(유료 소스 전환 대비).
- **그 외 전부: TypeScript** — 렌더러는 Remotion(React).
- Spring Boot는 넣지 않는다. 서비스화가 필요해지면 그때 붙인다.

### 4. 템플릿

화면에서 고른다. 최소 3종:

| 템플릿 | 적정 종목 수 |
| --- | --- |
| 단일 종목 레이아웃 | 1 |
| 라인 레이스 | 2~5 |
| 바 차트 레이스 | 8~15 |

종목 수에 따라 선택 가능한 템플릿을 제한하거나 추천한다.

## 반드시 지킬 것

### 데이터 정확도 — 여기서 대부분 틀린다

1. **수정주가(adjusted close) 필수.** 액면분할·배당 미반영이면 수익률이 그냥 틀린 숫자다.
2. **가격수익률 vs 총수익률(배당 재투자)** 을 구분하고 화면에 명시한다.
   단, KR 종목은 무료 소스(FDR/네이버, yfinance)에서 총수익 시계열을 구하기 어렵다.
3. **공통 거래일 축.** 한/미 휴장일이 다르다 → union 날짜축 + forward-fill.
4. **통화.** % 수익률 비교는 통화 무관. "원화 환산 수익률"을 쓰려면 환율 시계열 필요.
5. 상장폐지·티커 변경·생존 편향 주의.

일간 OHLCV는 확정 데이터다 → SQLite에 캐싱하고 증분 갱신. 렌더는 항상 로컬 DB에서 읽는다.

### 지수 데이터는 추종 ETF로 대체한다

KOSPI / S&P 500 / 나스닥100 등 지수는 지수사업자의 라이선스 대상이다.
가능한 한 추종 ETF를 쓴다: S&P500 → SPY/VOO, 나스닥100 → QQQ, 코스피200 → KODEX 200.

### YouTube 정책

- 발행량은 **하루 1개가 기본선**, 3개가 상한.
- YouTube는 반복적·대량생산 콘텐츠를 수익화 불가로 분류한다(2025-07-15~).
  "같은 템플릿에 티커만 바꾼 영상"이 정확히 여기 해당한다.
  → 영상마다 **맥락 한 줄**(왜 이 구간인지)을 반드시 넣는다. 템플릿 3종 분화도 이 대응의 일부.
- Data API v3: `videos.insert` = 1,600 units, 일 쿼터 10,000. 하루 1~3개면 쿼터는 문제없다.
- 미검증 OAuth 앱은 업로드 영상이 **locked private**으로 잠긴다. Studio 수동 전환으로도
  안 풀린다는 보고가 우세 → 검증 통과 전에는 업로드 자동화가 무용, 수동 업로드를 유지한다.
  앱 검증은 달력 시간(수 주)을 쓰므로 개발 초기에 미리 신청해둔다.

## 연출 품질 기준

이 프로젝트의 진짜 리스크는 아키텍처가 아니라 "45초짜리 영상이 볼 만한가"다.

- 1080×1920, 60fps, 30~50초
- y축 스케일이 튀지 않고 부드럽게 확장되는 easing ← 아마추어/프로를 가르는 지점
- 숫자 카운터 롤링, 라인 헤드 종목 배지
- 마지막 3초 결과 요약 정지 프레임
- 초반 1초 훅

## 로드맵 (현재 위치: P1 진행 중)

아래는 요약. 단계별 목표·산출물·완료 기준·진입 게이트는 위의 "전체 플랜" 노션 페이지가 원본이다.

| 단계 | 범위 |
| --- | --- |
| P0 | Python + matplotlib로 티커 2개 하드코딩 → mp4. **연출 감각 확보용** |
| P1 | Remotion 템플릿 1종 + Scene Spec 확정. 수동 업로드. 앱 검증 신청 |
| P2 | 데이터 캐시 DB + 어댑터 + 로컬 웹 UI |
| P3 | 템플릿 3종 + 화면 선택, 자동 자막·제목 생성 |
| P4 | YouTube API 업로드 자동화 |
| P5 | 모바일 앱 (뷰어 전용) |

P0 완료(2026-08-20), 게이트 4개 닫힘. 확정 연출 파라미터: 40초, 완급(선형+ease-out
블렌드 WARP 0.6), y축 EMA τ0.6s+클램프, 카운터 ₩ 환산+% 병기.
P1 개발 완료(ADR-002 승인, Remotion 파이프라인 동작) — 채널 개설·OAuth 검증 신청은 사용자 액션.
P2 진행 중 — 파이프라인·웹 스튜디오 완성, docker 이미지 빌드 검증만 남음.

## 개발 명령

- 데이터 적재: `.venv/bin/python3 data/collector/ingest.py 005930 NVDA USD/KRW` (증분, `--full`로 재수집)
- API 서버: `pnpm server` (:4600) / 렌더 워커: `pnpm queue` / 웹 스튜디오: `pnpm web` (:5173, /api 프록시)
- CLI 렌더: `pnpm render specs/<spec>.json` / Remotion Studio: `pnpm studio`
- BGM: `assets/bgm/`에 저작권 프리 트랙(YouTube 오디오 보관함)을 넣으면 웹 UI에서 선택 가능.
  Spec의 `bgm` 필드 → 워커가 ffmpeg로 먹싱. 플레이스홀더 재생성: `scripts/make-placeholder-bgm.py`
- 렌더러 A(Remotion)와 B(웹/앱)는 renderer-canvas의 단일 `drawScene`을 공유한다.
  그리기 코드는 거기에만 넣는다 — 페인터를 두 벌 만들면 웹/영상이 어긋난다.

## 열려 있는 질문

- 템플릿 3종의 구체 스펙(레이아웃, 강조 요소)
- 미검증 OAuth 앱의 locked private이 `videos.update`·Studio 어느 쪽으로도 안 풀리는지
  버리는 영상으로 실제 테스트 (안 풀린다는 보고가 우세)
- Remotion 5.0 라이선스 변경 내용 (현재 버전은 개인·3인 이하 조직 상업 이용 무료)
- 다운샘플링 방식 — 공유 시간 버킷 vs 기준 시리즈 LTTB 후 timestamp 재샘플
- KR 종목 총수익률 공백 대응 — 비교 영상은 전 종목 가격수익률로 통일할지
- 기간 중간 상장 종목의 리베이스 정책 — 상장일부터 리베이스 vs 입력 거부
